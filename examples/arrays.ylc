let a = [2.0,1.0,2.0,3.0]

a[3]

#let b = []
#let reduce = fn (f, acc, [head, ...tail]) {
#  if (head == null)  {acc} else { reduce(f, f(acc, head), tail) }
#}
#
#let map = fn (f, [head, ...tail]) {
#  if (head == null) { [] } else { [f(head), ...map(f, tail)] }
#}
