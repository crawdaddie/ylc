#import "libffi.so"
#let randint = extern fn (int max) int
#let randfloat = extern fn (double max) double

let mod_func = fn (x) {
  x + 1
}

let add2 = fn (x) {
  x + 2
}

let add3 = fn (x, y) {
  x + 3 + y
}
let x =2.0
